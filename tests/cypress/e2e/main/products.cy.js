/// <reference types="cypress" />

describe('products', () => {
  describe('cathegories', () => {
    // Shows header with cathegories and on list are demand products
    //? How to test if I mock?
  });

  describe('product elements', () => {
    // Checks if products has demand elements
  });

  describe('adding to basket', () => {
    // 
  });

  describe('available quantity', () => {

  });

  describe('pagination', () => {
    
  });
});
