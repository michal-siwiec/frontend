/// <reference types="cypress" />

describe('thank you page', () => {
  context('with cash on delivery payment method', () => {
    
  });

  context('with traditional bank trasfer payment method', () => {

  });
});
