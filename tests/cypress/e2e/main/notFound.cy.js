/// <reference types="cypress" />

describe('not found page', () => {
  describe('page elements', () => {

  });

  describe('redirects', () => {
    it('redirects to home page after several seconds', () => {

    });
  });
});
